# React Toronto Guide builds to default location
# LifePlanner static files are in public/LifePlanner/
# No special configuration needed - React handles static files

# React LifePlanner routing
[[redirects]]
  from = "/planner/*"
  to = "/Toronto-guide/planner/index.html"
  status = 200